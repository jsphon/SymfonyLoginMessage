<nav class="navbar navbar-default">
    <div class="container-fluid">
        <div class="navbar-header">
            <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#bs-example-navbar-collapse-1" aria-expanded="false">
                <span class="sr-only">Toggle navigation</span>
                <span class="icon-bar"></span>
                <span class="icon-bar"></span>
                <span class="icon-bar"></span>
            </button>
            <a class="navbar-brand" href="{{ path('homepage') }}">Workflow</a>
            <button type="button" class="btn btn-primary">Button1 <span class="label label-success label-as-badge">!</span></button>
            <button type="button" class="btn btn-primary">Button2 <span class="label label-info label-as-badge">!</span></button>
            <button type="button" class="btn btn-primary">Button3 <span class="label label-warning label-as-badge">!</span></button>           
            
            <a id='example' href="#" data-placement="bottom" data-toggle="popover" title="x" data-content="Why don't you click on button 4?">
                <button type="button" class="btn btn-primary">Button4 <span class="label label-success label-as-badge">!</span></button>
            </a>

        <script>
        $(document).ready(function(){
            $('[data-toggle="popover"]').popover(); 
            $("#example").popover({'placement':'bottom'}).popover('show');
            //http://stackoverflow.com/questions/13413057/how-to-insert-close-button-in-popover-for-bootstrap
            $('.popover-title').click(function(){
                $('#example').popover('destroy');
              });
        });
        </script>
        
        
            <form action="{{ path('user_flow_apply_transition') }}" method="post">
                <div class="btn-group-horizontal" role="group">
            
                    <button type="submit" name="transition" value="user_logs_in" class="btn btn-primary">
                           User Logs In
                    </button>
                    <button type="submit" name="transition" value="message_shown" class="btn btn-primary">
                           Message Shown
                    </button>

                </div>
            </form>

        </div>

        <div class="collapse navbar-collapse" id="bs-example-navbar-collapse-1">
            <ul class="nav navbar-nav navbar-right">
                {% if is_granted('ROLE_PREVIOUS_ADMIN') %}
                    <li>
                        <a href="{{ path(app.request.attributes.get('_route'), app.request.attributes.get('_route_params')|merge({_switch_user: '_exit'})) }}">
                            Hello {{ app.user.username }} | logout
                        </a>
                    </li>
                {% else %}
                    <li class="dropdown">
                        <a href="#" class="dropdown-toggle" data-toggle="dropdown" role="button" aria-haspopup="true" aria-expanded="false">
                            Connect as <span class="caret"></span>
                        </a>
                        <ul class="dropdown-menu">
                            {% for user in ['alice', 'journalist', 'spellchecker', 'admin'] %}
                                <li>
                                    <a href="{{ path(app.request.attributes.get('_route'), app.request.attributes.get('_route_params')|merge({_switch_user: user})) }}">
                                        {{ user|title }}
                                    </a>
                                </li>
                            {% endfor %}
                        </ul>
                    </li>
                {% endif %}
            </ul>
        </div>
    </div>
</nav>
